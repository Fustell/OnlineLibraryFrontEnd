const Endpoints = {
    AUTH: {
        LOGIN: "/auth/token/",
        REFRESH: "/auth/token/refresh/",
        LOGOUT: "/auth/logout/",
        PROFILE:"/auth/profile/"
    }
};

export default Endpoints;