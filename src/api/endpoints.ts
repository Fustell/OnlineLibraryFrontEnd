const Endpoints = {
    AUTH: {
        LOGIN: "/auth/token/",
        REFRESH: "/auth/token/refresh",
        LOGOUT: "/auth/token/logout",
        PROFILE:"/profile"
    }
};

export default Endpoints;