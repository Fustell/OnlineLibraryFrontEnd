const Endpoints = {
    AUTH: {
        LOGIN: "/auth/token/",
        REFRESH: "/auth/token/refresh/",
        LOGOUT: "/auth/logout/",
        PROFILE:"/profile/"
    }
};

export default Endpoints;